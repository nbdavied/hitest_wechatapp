{{question.question}}
<view wx:for="{{question.choices}}" wx:for-item="choice">
  <text bindtap='select' data-choiceId="{{choice.id}}" 
    class="{{selectClass[choice.id]}}">{{choice.text}}</text>
</view>